// import { previewList } from '../support/preview'
// // import previewList from '../support/preview'

// previewList.forEach((preview) => {
//   describe(`Cart  ${preview}`, () => {
//     beforeEach(() => {
//       cy.viewport(preview)
//     })
//     it('Should do a successful search', () => {
//       const fn1 = (arg1: string, arg2: number): string => {
//         return arg1
//       }

//       cy.visit('https://google.com')
//       cy.search('Cypress framework')
//     })
//     it('Should do a successful search', () => {
//       const fn1 = (arg1: string, arg2: number): string => {
//         return arg1
//       }
//       cy.visit('https://google.com')
//       cy.search('Cypress framework')
//     })
//   })
// })

// // const a = `Cart  ${preview}`
// // const b = 'Cart' + ' ' + preview
